import React from "react";
import styled from "styled-components";

const Footer = () => {
  return <FooterContainer>Hello from footer</FooterContainer>;
};

export default Footer;

const FooterContainer = styled.footer``;
